<Window x:Class="AutocadAutomation.View.ListComponents"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AutocadAutomation.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Модель" Binding="{Binding Path=Description}" Width="90" />
                <DataGridTextColumn Header="Компания" Binding="{Binding Path=Note}" Width="80" />
                <DataGridTextColumn Header="Цена" Binding="{Binding Path=PosItem}" Width="50" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>